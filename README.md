# ZstdScope

ZstdScope is a work-in-progress inspection toolkit for the Zstandard compressed data format.

The project is being designed in public. Initial requirements and architecture will be proposed through pull requests before implementation begins.
